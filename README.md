# Java-JDBC-Console-Based-Product-Management-System-PMS-
This project is a Command-Line Interface (CLI) application developed in Java that serves as a basic Product Management System (PMS). It uses Java Database Connectivity (JDBC) to perform standard CRUD (Create, Read, Update, Delete) operations on a dedicated MySQL database table (products).
